{
	"private": true,
	"name": "cra-templates-stocky37",
	"description": "Collection of CRA scripst & templates.",
	"license": "MIT",
	"author": "Tom Stockwell <stocky37@stockwells.me> (https://github.com/stocky37)",
	"repository": {
		"type": "git",
		"url": "https://github.com/stocky37/cra-templates.git"
	},
	"bugs": {
		"url": "https://github.com/stocky37/cra-templates/issues"
	},
	"version": "0.1.1",
	"workspaces": [
		"packages/*",
		"packages/template-*/dist"
	],
	"scripts": {
		"build": "yarn foreach:children run build:template && yarn",
		"clean": "yarn foreach:children run clean && yarn",
		"foreach": "yarn workspaces foreach -v -p --topological-dev ",
		"foreach:children": "yarn foreach --exclude cra-templates-stocky37",
		"foreach:publishable": "yarn foreach --include 'cra-template-*'",
		"foreach:templates": "yarn foreach --include 'template-*'",
		"foreach:versionable": "yarn foreach --exclude 'cra-template-!(scripts)'",
		"format": "prettier-package-json --use-tabs --write **/package.json && prettier --write .",
		"format:check": "prettier-package-json --use-tabs -l && prettier --check . ",
		"prepublish": "yarn release",
		"publish": "yarn foreach:publishable npm publish",
		"release": "bash scripts/release.sh",
		"test": "CI=true yarn foreach:children run test",
		"version:show": "yarn node scripts/showversion.js"
	},
	"devDependencies": {
		"fs-extra": "^9.0.1",
		"husky": "^4.2.5",
		"lint-staged": "^10.2.11",
		"prettier": "^2.0.5",
		"prettier-package-json": "^2.1.3",
		"pretty-quick": "^2.0.1"
	},
	"keywords": [
		"create-react-app",
		"react",
		"template"
	],
	"engines": {
		"node": ">=10"
	},
	"husky": {
		"hooks": {
			"pre-commit": "lint-staged && pretty-quick --staged"
		}
	},
	"lint-staged": {
		"package.json": "prettier-package-json --write"
	}
}
